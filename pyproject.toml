[project]
name = "pokedex"
version = "0.1.0"
description = "Simple Pokedex built with Python and PokeAPI"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py310"
